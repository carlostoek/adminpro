---
phase: 30-economy-shop-integration
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - bot/services/narrative.py
  - bot/services/container.py
autonomous: true

must_haves:
  truths:
    - NarrativeService.make_choice validates conditions before processing
    - NarrativeService.make_choice deducts besitos atomically for costly choices
    - NarrativeService.make_choice prevents double-spending on rapid clicks
    - NarrativeService delivers node rewards when user reaches a node
    - NarrativeService prevents replay farming by tracking delivered rewards
    - ServiceContainer injects wallet/reward/shop services into NarrativeService
  artifacts:
    - path: bot/services/narrative.py
      provides: Choice processing with cost deduction and reward delivery
      exports: ["make_choice", "_deduct_choice_cost", "_deliver_node_rewards"]
    - path: bot/services/container.py
      provides: Service injection for NarrativeService
      contains: "narrative.*wallet_service"
  key_links:
    - from: bot/services/narrative.py make_choice
      to: bot/services/wallet.py spend_besitos
      via: wallet_service.spend_besitos()
      pattern: "spend_besitos.*SPEND_STORY_CHOICE"
    - from: bot/services/narrative.py _deliver_node_rewards
      to: bot/services/reward.py claim_reward
      via: reward_service.claim_reward()
      pattern: "claim_reward"
---

<objective>
Implement choice cost processing and node reward delivery in NarrativeService.

Purpose: Enable the narrative system to charge besitos for choices and deliver rewards when users reach nodes, with protection against double-spending and replay farming.

Output: Updated NarrativeService with make_choice enhancements, cost deduction, reward delivery, and ServiceContainer integration.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.claude/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-economy-shop-integration/30-CONTEXT.md
@.planning/phases/30-economy-shop-integration/30-RESEARCH.md
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/services/narrative.py
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/services/container.py
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/services/wallet.py
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/services/reward.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add choice cost deduction method</name>
  <files>bot/services/narrative.py</files>
  <action>
    Add _deduct_choice_cost method to NarrativeService for atomic besitos deduction.

    Method signature:
    async def _deduct_choice_cost(
        self,
        user_id: int,
        choice: StoryChoice,
        user_role: str
    ) -> Tuple[bool, str, Optional[Transaction]]:

    Implementation:
    1. Calculate cost using calculate_choice_cost(choice, user_role)
    2. If cost == 0, return (True, "no_cost", None)
    3. Check if wallet_service is available, if not return (False, "wallet_service_unavailable", None)
    4. Call wallet_service.spend_besitos() with:
       - transaction_type=TransactionType.SPEND_STORY_CHOICE
       - reason=f"Story choice #{choice.id}: {choice.choice_text[:50]}"
       - metadata including choice_id, source_node_id, target_node_id
    5. Return the result from spend_besitos

    This method handles atomic cost deduction with proper transaction logging.
  </action>
  <verify>grep -n "_deduct_choice_cost" bot/services/narrative.py | head -3</verify>
  <done>NarrativeService has _deduct_choice_cost method that uses wallet_service</done>
</task>

<task type="auto">
  <name>Task 2: Add node reward delivery method</name>
  <files>bot/services/narrative.py</files>
  <action>
    Add _deliver_node_rewards method to NarrativeService for delivering rewards when user reaches a node.

    Method signature:
    async def _deliver_node_rewards(
        self,
        user_id: int,
        node: StoryNode
    ) -> Tuple[bool, List[Dict]]:

    Implementation:
    1. Query node.attached_rewards for active rewards (is_active=True)
    2. If no rewards or reward_service unavailable, return (True, [])
    3. For each NodeReward:
       - Skip if not node_reward.is_active
       - Check if reward is_repeatable or user hasn't claimed it before
       - Call reward_service.claim_reward(user_id, reward.id)
       - Track result in list
    4. Return (all_rewards_delivered: bool, results: List[Dict])

    Each result dict should contain:
    - reward_id, reward_name, success, message, details

    This prevents replay farming by checking claim_count before delivering.
  </action>
  <verify>grep -n "_deliver_node_rewards" bot/services/narrative.py | head -3</verify>
  <done>NarrativeService has _deliver_node_rewards method using reward_service</done>
</task>

<task type="auto">
  <name>Task 3: Add batched reward notification builder</name>
  <files>bot/services/narrative.py</files>
  <action>
    Add build_reward_notification method to NarrativeService for batched reward messages.

    Method signature:
    def build_reward_notification(
        self,
        reward_results: List[Dict],
        node_name: Optional[str] = None
    ) -> str:

    Implementation:
    1. Filter for successful rewards only
    2. If no successful rewards, return empty string
    3. Build message with Diana's voice (🫦) for user-facing content:
       - Single reward: "🫦 <b>Sorpresa...</b>\n\nHas recibido: [reward description]"
       - Multiple rewards: "🫦 <b>Qué suerte...</b>\n\nHas recibido:\n- [reward 1]\n- [reward 2]"
    4. Format reward descriptions based on type:
       - BESITOS: "💰 +X besitos"
       - VIP_EXTENSION: "⭐ +X días VIP"
       - CONTENT: "🎁 Contenido exclusivo"
       - BADGE: "🏆 Insignia [name]"
    5. Return formatted HTML message

    This provides the batched summary UX per user decision in CONTEXT.md.
  </action>
  <verify>grep -n "build_reward_notification" bot/services/narrative.py | head -3</verify>
  <done>NarrativeService has build_reward_notification method for batched rewards</done>
</task>

<task type="auto">
  <name>Task 4: Enhance make_choice with cost and rewards</name>
  <files>bot/services/narrative.py</files>
  <action>
    Enhance the existing make_choice method to integrate cost deduction and reward delivery.

    Update the method signature to accept user_role:
    async def make_choice(
        self,
        user_id: int,
        progress: UserStoryProgress,
        choice_id: int,
        user_role: str = "FREE"
    ) -> Tuple[bool, str, Optional[StoryNode], Optional[UserStoryProgress], Dict]:

    The return value now includes a dict with additional info:
    - cost_deducted: bool
    - cost_amount: int
    - rewards_delivered: List[Dict]
    - notification_text: Optional[str]

    Enhance the implementation:
    1. After validating the choice (step 3), add condition validation:
       - Call evaluate_choice_conditions(user_id, choice)
       - If not can_access, return (False, "conditions_not_met", None, None, {"missing_requirements": missing})

    2. After validation, deduct cost:
       - Call _deduct_choice_cost(user_id, choice, user_role)
       - If not success and msg == "insufficient_funds", return (False, "insufficient_funds", None, None, {"required": cost, "balance": balance})
       - Track cost_deducted and cost_amount

    3. After advancing to target node, deliver rewards:
       - Call _deliver_node_rewards(user_id, target_node)
       - Build notification text with build_reward_notification
       - Track rewards_delivered

    4. Return the enhanced result tuple

    This integrates all economy features into the core choice flow.
  </action>
  <verify>grep -A 5 "async def make_choice" bot/services/narrative.py | head -10</verify>
  <done>make_choice validates conditions, deducts costs, and delivers rewards atomically</done>
</task>

<task type="auto">
  <name>Task 5: Update ServiceContainer for NarrativeService injection</name>
  <files>bot/services/container.py</files>
  <action>
    Update ServiceContainer to properly inject dependencies into NarrativeService.

    Find the narrative property in ServiceContainer and update it from:
    @property
    def narrative(self) -> NarrativeService:
        if self._narrative is None:
            self._narrative = NarrativeService(self.session)
        return self._narrative

    To:
    @property
    def narrative(self) -> NarrativeService:
        if self._narrative is None:
            self._narrative = NarrativeService(
                self.session,
                wallet_service=self.wallet,
                reward_service=self.reward,
                shop_service=self.shop,
                streak_service=self.streak
            )
        return self._narrative

    Ensure all injected services (wallet, reward, shop, streak) have their properties defined before narrative in the container.

    This ensures NarrativeService has access to all economy services when used through the container.
  </action>
  <verify>grep -A 10 "def narrative" bot/services/container.py | head -15</verify>
  <done>ServiceContainer injects all required services into NarrativeService</done>
</task>

</tasks>

<verification>
- _deduct_choice_cost method exists and uses wallet_service.spend_besitos
- _deliver_node_rewards method exists and uses reward_service.claim_reward
- build_reward_notification method exists for batched reward messages
- make_choice validates conditions before processing
- make_choice deducts cost atomically with SPEND_STORY_CHOICE transaction type
- make_choice delivers node rewards after advancing
- ServiceContainer injects wallet, reward, shop, and streak services into NarrativeService
</verification>

<success_criteria>
NarrativeService can process choices with cost deduction, condition validation, and reward delivery. The ServiceContainer properly wires all dependencies. The choice flow is atomic and protected against double-spending.
</success_criteria>

<output>
After completion, create `.planning/phases/30-economy-shop-integration/30-02-SUMMARY.md`
</output>
