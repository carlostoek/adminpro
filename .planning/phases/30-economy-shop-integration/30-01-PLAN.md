---
phase: 30-economy-shop-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/database/enums.py
  - bot/database/models.py
  - bot/services/narrative.py
autonomous: true

must_haves:
  truths:
    - TransactionType enum includes SPEND_STORY_CHOICE for choice cost audit trail
    - TransactionType enum includes EARN_STORY_COMPLETION for story reward audit trail
    - StoryChoice model has vip_cost_besitos field for VIP discount flexibility
    - ShopProduct model has unlocks_node_id field for shop-node linkage
    - NarrativeService has wallet_service and reward_service injected
    - NarrativeService can evaluate choice conditions using cascading AND/OR logic
  artifacts:
    - path: bot/database/enums.py
      provides: TransactionType with SPEND_STORY_CHOICE and EARN_STORY_COMPLETION
      contains: "SPEND_STORY_CHOICE"
    - path: bot/database/models.py
      provides: StoryChoice with vip_cost_besitos field
      contains: "vip_cost_besitos"
    - path: bot/database/models.py
      provides: ShopProduct with unlocks_node_id field
      contains: "unlocks_node_id"
    - path: bot/services/narrative.py
      provides: Extended NarrativeService with economy integration
      exports: ["evaluate_choice_access", "calculate_choice_cost"]
  key_links:
    - from: bot/services/narrative.py
      to: bot/services/wallet.py
      via: wallet_service injection
      pattern: "wallet_service.*spend_besitos"
    - from: bot/services/narrative.py
      to: bot/services/reward.py
      via: reward_service injection
      pattern: "reward_service.*claim_reward"
---

<objective>
Extend database models and NarrativeService foundation to support economy integration.

Purpose: Create the data structures and service foundation needed for choice costs, VIP discounts, shop-node linkage, and service injection patterns.

Output: Updated enums, models with new fields, and extended NarrativeService with dependency injection setup.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.claude/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-economy-shop-integration/30-CONTEXT.md
@.planning/phases/30-economy-shop-integration/30-RESEARCH.md
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/database/enums.py
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/database/models.py
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/services/narrative.py
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/services/wallet.py
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/services/reward.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend TransactionType enum</name>
  <files>bot/database/enums.py</files>
  <action>
    Add two new transaction types to TransactionType enum:
    1. SPEND_STORY_CHOICE = "SPEND_STORY_CHOICE" - For when users spend besitos on story choices
    2. EARN_STORY_COMPLETION = "EARN_STORY_COMPLETION" - For when users earn besitos from completing stories

    Add display_name entries for both in the display_name property:
    - SPEND_STORY_CHOICE: "Elección en Historia"
    - EARN_STORY_COMPLETION: "Completar Historia"

    Follow the existing pattern in the enum. Place SPEND_STORY_CHOICE after SPEND_ADMIN and EARN_STORY_COMPLETION after EARN_ADMIN.
  </action>
  <verify>grep -E "SPEND_STORY_CHOICE|EARN_STORY_COMPLETION" bot/database/enums.py | wc -l returns 2</verify>
  <done>TransactionType enum includes both new types with proper display names</done>
</task>

<task type="auto">
  <name>Task 2: Add vip_cost_besitos to StoryChoice model</name>
  <files>bot/database/models.py</files>
  <action>
    Add vip_cost_besitos field to StoryChoice model (around line 1787, after cost_besitos).

    Field definition:
    - vip_cost_besitos = Column(Integer, nullable=True)
    - Comment: "Precio VIP opcional (si es null, se usa cost_besitos)"

    Update the is_free property to consider vip_cost_besitos:
    - Both cost_besitos == 0 AND vip_cost_besitos in (None, 0) means free

    This allows stories to have VIP discounts on choices. If vip_cost_besitos is null, VIP users pay the regular cost_besitos price.
  </action>
  <verify>grep -n "vip_cost_besitos" bot/database/models.py | head -3</verify>
  <done>StoryChoice model has vip_cost_besitos field and updated is_free property</done>
</task>

<task type="auto">
  <name>Task 3: Add unlocks_node_id to ShopProduct model</name>
  <files>bot/database/models.py</files>
  <action>
    Add unlocks_node_id field to ShopProduct model (around line 1072, after purchase_count).

    Field definition:
    - unlocks_node_id = Column(Integer, ForeignKey("story_nodes.id", ondelete="SET NULL"), nullable=True, index=True)
    - Comment: "Nodo de historia desbloqueado al comprar este producto"

    Also add the relationship to StoryNode (add to relationships section around line 1088):
    - unlocked_node = relationship("StoryNode", foreign_keys=[unlocks_node_id], lazy="selectin")

    This enables shop products to unlock specific story nodes as a purchase bonus (SHOP-01 requirement).
  </action>
  <verify>grep -n "unlocks_node_id" bot/database/models.py | head -3</verify>
  <done>ShopProduct model has unlocks_node_id field with foreign key and relationship</done>
</task>

<task type="auto">
  <name>Task 4: Extend NarrativeService with service injection</name>
  <files>bot/services/narrative.py</files>
  <action>
    Extend NarrativeService __init__ to accept optional service dependencies following the pattern from RewardService.

    Update the __init__ signature from:
    def __init__(self, session: AsyncSession):

    To:
    def __init__(
        self,
        session: AsyncSession,
        wallet_service=None,
        reward_service=None,
        shop_service=None,
        streak_service=None
    ):

    Store the services as instance attributes:
    self.wallet_service = wallet_service
    self.reward_service = reward_service
    self.shop_service = shop_service
    self.streak_service = streak_service

    Update the docstring to document the new parameters.

    This enables the narrative service to access economy, reward, shop, and streak functionality.
  </action>
  <verify>grep -n "wallet_service\|reward_service\|shop_service\|streak_service" bot/services/narrative.py | head -10</verify>
  <done>NarrativeService accepts and stores wallet, reward, shop, and streak service dependencies</done>
</task>

<task type="auto">
  <name>Task 5: Add choice condition evaluation methods</name>
  <files>bot/services/narrative.py</files>
  <action>
    Add methods to NarrativeService for evaluating choice conditions using the cascading AND/OR pattern from RewardService.

    Add these methods after the existing methods:

    1. async def evaluate_choice_conditions(self, user_id: int, choice: StoryChoice) -> Tuple[bool, List[str]]:
       - Evaluate if user can access a choice based on its conditions
       - Parse choice.conditions JSON (list of condition objects)
       - Support condition types: level, streak, product_owned, total_earned
       - Return (can_access: bool, missing_requirements: List[str])
       - Use injected services for checking (wallet_service for level/total_earned, streak_service for streak, shop_service for product_owned)

    2. async def calculate_choice_cost(self, choice: StoryChoice, user_role: str) -> int:
       - Calculate the cost for a user to make this choice
       - If user_role == "VIP" and choice.vip_cost_besitos is not None, return vip_cost_besitos
       - Otherwise return choice.cost_besitos

    3. def _format_requirement_message(self, missing: List[str]) -> str:
       - Format missing requirements into user-friendly message
       - Use Lucien's voice (formal, elegant)
       - Example: "Necesitas: nivel 5 Y racha 7 días"

    Follow the pattern from RewardService.evaluate_reward_conditions() for condition evaluation logic.
  </action>
  <verify>grep -n "evaluate_choice_conditions\|calculate_choice_cost\|_format_requirement_message" bot/services/narrative.py | head -5</verify>
  <done>NarrativeService has condition evaluation and cost calculation methods</done>
</task>

</tasks>

<verification>
- TransactionType enum has SPEND_STORY_CHOICE and EARN_STORY_COMPLETION
- StoryChoice model has vip_cost_besitos field
- ShopProduct model has unlocks_node_id field with foreign key
- NarrativeService accepts wallet, reward, shop, and streak services
- NarrativeService has evaluate_choice_conditions method
- NarrativeService has calculate_choice_cost method
</verification>

<success_criteria>
All database model changes are in place and NarrativeService has the foundation methods for economy integration. The service can evaluate conditions and calculate costs, ready for the choice processing implementation in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/30-economy-shop-integration/30-01-SUMMARY.md`
</output>
