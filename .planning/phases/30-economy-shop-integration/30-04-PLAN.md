---
phase: 30-economy-shop-integration
plan: 04
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - bot/services/shop.py
  - bot/services/story_editor.py
autonomous: true

must_haves:
  truths:
    - ShopService.purchase_product unlocks story nodes when product has unlocks_node_id
    - StoryEditorService can attach rewards to nodes
    - StoryEditorService can set choice costs and VIP discounts
    - StoryEditorService can configure choice conditions
    - Node access can require product ownership
  artifacts:
    - path: bot/services/shop.py
      provides: Shop purchase with node unlocking
      exports: ["purchase_product"]
    - path: bot/services/story_editor.py
      provides: Admin story configuration with economy
      exports: ["set_choice_cost", "attach_reward_to_node", "set_choice_conditions"]
  key_links:
    - from: bot/services/shop.py purchase_product
      to: bot/database/models.py UserStoryProgress
      via: Create progress record with unlocked node
      pattern: "unlocks_node_id"
---

<objective>
Implement shop-node integration and admin story editor enhancements for economy features.

Purpose: Enable shop products to unlock story nodes and provide admin tools for configuring choice costs, rewards, and conditions.

Output: Updated ShopService with node unlocking and StoryEditorService with economy configuration methods.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.claude/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-economy-shop-integration/30-CONTEXT.md
@.planning/phases/30-economy-shop-integration/30-RESEARCH.md
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/services/shop.py
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/services/story_editor.py
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/database/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add node unlocking to shop purchase</name>
  <files>bot/services/shop.py</files>
  <action>
    Enhance ShopService.purchase_product to unlock story nodes when product has unlocks_node_id.

    After successful purchase (after creating UserContentAccess record):
    1. Check if product.unlocks_node_id is not None
    2. If set:
       - Import NarrativeService (avoid circular import with local import)
       - Check if user already has progress for the story containing this node
       - If no progress exists, create one using narrative_service.start_story
       - Update progress to set current_node_id to the unlocked node
       - Log the node unlock event
    3. Add to return result:
       - "unlocked_node_id": product.unlocks_node_id (if applicable)
       - "unlocked_story_id": story_id (if applicable)

    This implements SHOP-01: Shop products can unlock specific story nodes as purchase bonus.
  </action>
  <verify>grep -n "unlocks_node_id" bot/services/shop.py | head -5</verify>
  <done>Shop purchase unlocks story nodes when product has unlocks_node_id</done>
</task>

<task type="auto">
  <name>Task 2: Add set_choice_cost method to StoryEditorService</name>
  <files>bot/services/story_editor.py</files>
  <action>
    Add set_choice_cost method to StoryEditorService for admin configuration of choice costs.

    Method signature:
    async def set_choice_cost(
        self,
        choice_id: int,
        cost_besitos: int,
        vip_cost_besitos: Optional[int] = None
    ) -> Tuple[bool, str]:

    Implementation:
    1. Validate cost_besitos >= 0
    2. If vip_cost_besitos is not None, validate vip_cost_besitos >= 0
    3. Fetch StoryChoice by ID
    4. If not found, return (False, "choice_not_found")
    5. Update choice.cost_besitos = cost_besitos
    6. Update choice.vip_cost_besitos = vip_cost_besitos
    7. Flush session
    8. Return (True, "cost_updated")

    This allows admins to set choice costs and VIP discounts via the editor.
  </action>
  <verify>grep -n "set_choice_cost" bot/services/story_editor.py</verify>
  <done>StoryEditorService has set_choice_cost method for admin configuration</done>
</task>

<task type="auto">
  <name>Task 3: Add attach_reward_to_node method</name>
  <files>bot/services/story_editor.py</files>
  <action>
    Add attach_reward_to_node method to StoryEditorService for attaching rewards to story nodes.

    Method signature:
    async def attach_reward_to_node(
        self,
        node_id: int,
        reward_id: int
    ) -> Tuple[bool, str, Optional[NodeReward]]:

    Implementation:
    1. Validate node exists and is active
    2. Validate reward exists and is active
    3. Check if attachment already exists (query NodeReward)
    4. If exists and is_active=False, reactivate it
    5. If exists and is_active=True, return (False, "already_attached", existing)
    6. If not exists, create new NodeReward record
    7. Flush session
    8. Return (True, "reward_attached", node_reward)

    This implements ECON-03: Story nodes can unlock rewards when reached.
  </action>
  <verify>grep -n "attach_reward_to_node" bot/services/story_editor.py</verify>
  <done>StoryEditorService can attach rewards to nodes</done>
</task>

<task type="auto">
  <name>Task 4: Add set_choice_conditions method</name>
  <files>bot/services/story_editor.py</files>
  <action>
    Add set_choice_conditions method to StoryEditorService for configuring choice access conditions.

    Method signature:
    async def set_choice_conditions(
        self,
        choice_id: int,
        conditions: List[Dict]
    ) -> Tuple[bool, str]:

    The conditions parameter is a list of dicts with:
    - type: "level" | "streak" | "product_owned" | "total_earned"
    - value: int (for level, streak, total_earned) or int (product_id for product_owned)
    - group: int (0 for AND, 1+ for OR groups)

    Implementation:
    1. Fetch StoryChoice by ID
    2. If not found, return (False, "choice_not_found")
    3. Validate each condition:
       - type must be valid
       - value must be appropriate for type
       - group must be >= 0
    4. Store conditions as JSON in choice.conditions
    5. Flush session
    6. Return (True, "conditions_updated")

    This implements ECON-05: Choices can have conditions via extended condition system.
  </action>
  <verify>grep -n "set_choice_conditions" bot/services/story_editor.py</verify>
  <done>StoryEditorService can set choice conditions</done>
</task>

<task type="auto">
  <name>Task 5: Add remove_reward_from_node method</name>
  <files>bot/services/story_editor.py</files>
  <action>
    Add remove_reward_from_node method to StoryEditorService for detaching rewards from nodes.

    Method signature:
    async def remove_reward_from_node(
        self,
        node_id: int,
        reward_id: int
    ) -> Tuple[bool, str]:

    Implementation:
    1. Query NodeReward where node_id and reward_id match
    2. If not found, return (False, "attachment_not_found")
    3. Set is_active = False (soft delete)
    4. Flush session
    5. Return (True, "reward_detached")

    This allows admins to remove rewards from nodes without deleting the records.
  </action>
  <verify>grep -n "remove_reward_from_node" bot/services/story_editor.py</verify>
  <done>StoryEditorService can detach rewards from nodes</done>
</task>

<task type="auto">
  <name>Task 6: Add get_node_rewards method</name>
  <files>bot/services/story_editor.py</files>
  <action>
    Add get_node_rewards method to StoryEditorService for listing rewards attached to a node.

    Method signature:
    async def get_node_rewards(
        self,
        node_id: int
    ) -> List[Dict]:

    Implementation:
    1. Query NodeReward records for node_id where is_active=True
    2. Eager load reward relationship
    3. Return list of dicts with:
       - node_reward_id
       - reward_id
       - reward_name
       - reward_type
       - reward_value
       - is_repeatable
       - created_at

    This allows admins to view which rewards are attached to a node.
  </action>
  <verify>grep -n "get_node_rewards" bot/services/story_editor.py</verify>
  <done>StoryEditorService can list rewards attached to a node</done>
</task>

</tasks>

<verification>
- Shop purchase unlocks story nodes when product has unlocks_node_id
- StoryEditorService can set choice costs and VIP discounts
- StoryEditorService can attach rewards to nodes
- StoryEditorService can set choice conditions
- StoryEditorService can detach rewards from nodes
- StoryEditorService can list node rewards
</verification>

<success_criteria>
Shop products can unlock story nodes as purchase bonuses. Admins can configure choice costs, VIP discounts, conditions, and node rewards through the StoryEditorService. All economy features are configurable via the admin interface.
</success_criteria>

<output>
After completion, create `.planning/phases/30-economy-shop-integration/30-04-SUMMARY.md`
</output>
