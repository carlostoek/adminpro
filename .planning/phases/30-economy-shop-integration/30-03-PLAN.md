---
phase: 30-economy-shop-integration
plan: 03
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - bot/handlers/user/story_handlers.py
  - bot/utils/keyboards.py
autonomous: true

must_haves:
  truths:
    - Story choice buttons show 🔒 emoji for costly choices
    - Story choice buttons show 🚫 emoji for condition-locked choices
    - Cost is displayed in confirmation dialog with VIP discount shown
    - Insufficient balance shows Lucien's voice message with recovery path
    - Confirmation dialog appears before charging for costly choices
    - Batched reward notification is shown after reaching node
  artifacts:
    - path: bot/handlers/user/story_handlers.py
      provides: Choice display and confirmation handlers
      exports: ["handle_story_choice", "show_choice_confirmation", "handle_insufficient_funds"]
    - path: bot/utils/keyboards.py
      provides: Choice keyboards with lock indicators
      exports: ["build_story_choices_keyboard"]
  key_links:
    - from: bot/handlers/user/story_handlers.py
      to: bot/services/narrative.py
      via: container.narrative.evaluate_choice_conditions
      pattern: "evaluate_choice_conditions"
    - from: bot/handlers/user/story_handlers.py
      to: bot/services/narrative.py
      via: container.narrative.calculate_choice_cost
      pattern: "calculate_choice_cost"
---

<objective>
Implement user-facing handlers for story choices with cost display and confirmation dialogs.

Purpose: Create the UI layer for economy integration including choice buttons with lock indicators, confirmation dialogs showing costs, and elegant insufficient balance messages.

Output: Updated story handlers with choice display logic and confirmation flow.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.claude/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-economy-shop-integration/30-CONTEXT.md
@.planning/phases/30-economy-shop-integration/30-RESEARCH.md
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/handlers/user/story_handlers.py
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/utils/keyboards.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update choice keyboard builder with lock indicators</name>
  <files>bot/utils/keyboards.py</files>
  <action>
    Update build_story_choices_keyboard function to show lock indicators and handle choice states.

    Current signature likely:
    def build_story_choices_keyboard(choices: List[StoryChoice], story_id: int) -> InlineKeyboardMarkup

    Update to:
    def build_story_choices_keyboard(
        choices: List[StoryChoice],
        story_id: int,
        choice_states: Optional[List[Dict]] = None
    ) -> InlineKeyboardMarkup

    The choice_states parameter is a list of dicts with:
    - choice_id: int
    - state: "available" | "costly" | "condition_locked"
    - cost: Optional[int]
    - vip_cost: Optional[int]
    - missing_requirements: List[str]

    Update button text logic:
    - Available choice: f"{choice.choice_text[:40]}"
    - Costly choice (can afford): f"🔒 {choice.choice_text[:37]}"
    - Costly choice (cannot afford): f"🔒 {choice.choice_text[:37]}"
    - Condition locked: f"🚫 {choice.choice_text[:37]}"

    Callback data remains: f"story:choice:{story_id}:{choice.id}"

    If choice_states is None (backward compatibility), show all choices without indicators.

    This implements the visual indicator requirements from CONTEXT.md.
  </action>
  <verify>grep -n "def build_story_choices_keyboard" bot/utils/keyboards.py</verify>
  <done>Keyboard builder accepts choice_states and shows appropriate lock emojis</done>
</task>

<task type="auto">
  <name>Task 2: Add choice state preparation method</name>
  <files>bot/handlers/user/story_handlers.py</files>
  <action>
    Add _prepare_choice_states helper method to prepare choice display states.

    Method signature:
    async def _prepare_choice_states(
        self,
        user_id: int,
        choices: List[StoryChoice],
        user_role: str
    ) -> List[Dict]:

    Implementation:
    1. Get user balance from wallet_service (via container)
    2. For each choice:
       - Call container.narrative.evaluate_choice_conditions(user_id, choice)
       - Call container.narrative.calculate_choice_cost(choice, user_role)
       - Determine state:
         * If not can_access: state="condition_locked"
         * Elif cost > 0: state="costly"
         * Else: state="available"
       - Build dict with choice_id, state, cost, vip_cost, missing_requirements
    3. Return list of choice state dicts

    This prepares the data needed for the keyboard builder.
  </action>
  <verify>grep -n "_prepare_choice_states" bot/handlers/user/story_handlers.py</verify>
  <done>_prepare_choice_states method exists and evaluates conditions/costs</done>
</task>

<task type="auto">
  <name>Task 3: Add choice confirmation dialog handler</name>
  <files>bot/handlers/user/story_handlers.py</files>
  <action>
    Add handle_choice_confirmation callback handler for costly choices.

    This handler is triggered when user selects a costly choice.

    Flow:
    1. Extract choice_id from callback data
    2. Get choice details and calculate cost
    3. Build confirmation message with Lucien's voice (🎩):
       - "🎩 <b>Confirmar decisión</b>\n\n"
       - f"{choice.choice_text}\n\n"
       - If VIP with discount: "Costo: 40 besitos (VIP: era 50)"
       - Else: "Costo: {cost} besitos"
       - f"\nTu balance: {balance} besitos"
       - "\n\n<i>¿Desea proceder?</i>"
    4. Show confirmation keyboard:
       - "✅ Confirmar" -> story:confirm:{story_id}:{choice_id}
       - "❌ Cancelar" -> story:cancel:{story_id}
    5. Update message with confirmation dialog

    This implements the cost display in confirmation dialog requirement.
  </action>
  <verify>grep -n "handle_choice_confirmation\|story:confirm" bot/handlers/user/story_handlers.py</verify>
  <done>Choice confirmation dialog shows cost and VIP discount before charging</done>
</task>

<task type="auto">
  <name>Task 4: Add insufficient funds handler</name>
  <files>bot/handlers/user/story_handlers.py</files>
  <action>
    Add handle_insufficient_funds method for elegant insufficient balance UX.

    Method signature:
    async def handle_insufficient_funds(
        self,
        message_or_callback,
        required: int,
        balance: int
    ) -> None:

    Implementation:
    1. Build message with Lucien's voice (🎩) per CONTEXT.md:
       "🎩 <b>Atención</b>\n\n"
       f"Para esta decisión necesita hacer una inversión de {required} besitos, "
       f"ahora cuenta con {balance} besitos.\n\n"
       "Le sugiero que vaya a reclamar su regalo del día, tal vez con eso pueda acceder."
    2. Show recovery keyboard:
       - "🎁 Ir a regalo diario" -> menu:daily_gift
       - "💰 Cómo ganar besitos" -> menu:economy
       - "🔙 Volver" -> story:back:{story_id}
    3. Send message with keyboard

    This implements the elegant insufficient balance message with recovery path.
  </action>
  <verify>grep -n "handle_insufficient_funds\|inversión de.*besitos" bot/handlers/user/story_handlers.py</verify>
  <done>Insufficient funds shows Lucien's elegant message with recovery buttons</done>
</task>

<task type="auto">
  <name>Task 5: Update choice selection flow</name>
  <files>bot/handlers/user/story_handlers.py</files>
  <action>
    Update the existing choice selection handler to integrate economy flow.

    Update the handler that processes story:choice callbacks:

    1. Extract choice_id from callback
    2. Get choice and calculate cost
    3. Check conditions using container.narrative.evaluate_choice_conditions
       - If condition_locked: show requirements message and return
    4. If cost > 0:
       - Show confirmation dialog (Task 3)
       - Return (don't process choice yet)
    5. If cost == 0:
       - Process choice immediately using container.narrative.make_choice
       - Handle result:
         * insufficient_funds: call handle_insufficient_funds
         * conditions_not_met: show requirements
         * success: show target node content
       - If rewards_delivered: show batched notification

    This integrates the full economy flow into the existing choice handler.
  </action>
  <verify>grep -n "story:choice" bot/handlers/user/story_handlers.py | head -5</verify>
  <done>Choice selection handler integrates condition checks, confirmation, and economy flow</done>
</task>

<task type="auto">
  <name>Task 6: Add confirm choice handler</name>
  <files>bot/handlers/user/story_handlers.py</files>
  <action>
    Add handler for story:confirm callback to process confirmed costly choices.

    Handler signature:
    async def handle_confirm_choice(callback: CallbackQuery, state: FSMContext)

    Implementation:
    1. Extract story_id and choice_id from callback data
    2. Get user progress and choice
    3. Call container.narrative.make_choice with user_role
    4. Handle results:
       - insufficient_funds: call handle_insufficient_funds
       - success: show target node content and rewards notification
    5. Update message with result

    This completes the confirmation flow for costly choices.
  </action>
  <verify>grep -n "handle_confirm_choice\|story:confirm" bot/handlers/user/story_handlers.py</verify>
  <done>Confirm choice handler processes costly choices after user confirmation</done>
</task>

</tasks>

<verification>
- Choice keyboards show 🔒 for costly and 🚫 for condition-locked choices
- Cost is displayed in confirmation dialog with VIP discount format
- Insufficient balance shows Lucien's voice message with recovery path
- Confirmation dialog appears before charging for costly choices
- Batched reward notification is shown after reaching node
</verification>

<success_criteria>
Users see choice buttons with appropriate lock indicators, get confirmation dialogs for costly choices, receive elegant insufficient balance messages, and see batched reward notifications. The flow matches the UX decisions in CONTEXT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/30-economy-shop-integration/30-03-SUMMARY.md`
</output>
