---
phase: 27-core-narrative-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/database/enums.py
  - bot/database/models.py
autonomous: true

must_haves:
  truths:
    - "StoryStatus enum exists with DRAFT, PUBLISHED, ARCHIVED values"
    - "NodeType enum exists with START, STORY, CHOICE, ENDING values"
    - "StoryProgressStatus enum exists with ACTIVE, PAUSED, COMPLETED, ABANDONED values"
    - "Story model exists with id, title, description, is_premium, status fields"
    - "StoryNode model exists with id, story_id, node_type, content_text, media_file_ids, tier_required fields"
    - "StoryChoice model exists with id, source_node_id, target_node_id, choice_text, cost_besitos fields"
    - "UserStoryProgress model exists with id, user_id, story_id, current_node_id, decisions_made, status fields"
  artifacts:
    - path: "bot/database/enums.py"
      provides: "StoryStatus, NodeType, StoryProgressStatus enums"
      contains: ["class StoryStatus", "class NodeType", "class StoryProgressStatus"]
    - path: "bot/database/models.py"
      provides: "Story, StoryNode, StoryChoice, UserStoryProgress models"
      contains: ["class Story", "class StoryNode", "class StoryChoice", "class UserStoryProgress"]
  key_links:
    - from: "Story"
      to: "StoryNode"
      via: "one-to-many relationship"
    - from: "StoryNode"
      to: "StoryChoice"
      via: "one-to-many relationship (source_node)"
    - from: "StoryChoice"
      to: "StoryNode"
      via: "self-referential many-to-one (target_node)"
    - from: "UserStoryProgress"
      to: "Story"
      via: "many-to-one relationship"
    - from: "UserStoryProgress"
      to: "StoryNode"
      via: "many-to-one relationship (current_node)"
---

<objective>
Create database enums and models for the narrative system.

Purpose: Foundation for story storage, retrieval, and user progress tracking. These models enable the core narrative functionality where users can experience interactive stories with choices.
Output: Extended enums.py with narrative enums and models.py with Story, StoryNode, StoryChoice, and UserStoryProgress models.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.claude/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/database/enums.py
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/database/models.py
@/data/data/com.termux/files/home/repos/adminpro-narrativa2/bot/database/base.py

The codebase uses SQLAlchemy 2.0 with declarative base pattern. Models follow these conventions:
- Primary keys use Integer with autoincrement=True
- Foreign keys use proper type matching (Integer for model IDs, BigInteger for user_ids)
- Timestamps use DateTime with lambda defaults for UTC
- JSON columns for flexible data (decisions_made, media_file_ids)
- Relationships use lazy="selectin" for eager loading
- Indexes defined in __table_args__ for query optimization
- Enums are Python Enum classes with str mixin for database storage
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add narrative enums to enums.py</name>
  <files>bot/database/enums.py</files>
  <action>
Add three new enums at the end of bot/database/enums.py:

1. StoryStatus(enum.Enum): Lifecycle states for stories
   - DRAFT = "draft" (being created/edited)
   - PUBLISHED = "published" (available to users)
   - ARCHIVED = "archived" (no longer available)
   Add display_name property and emoji property

2. NodeType(enum.Enum): Types of story nodes
   - START = "start" (entry point of story)
   - STORY = "story" (narrative content node)
   - CHOICE = "choice" (decision point)
   - ENDING = "ending" (terminal node)
   Add display_name property

3. StoryProgressStatus(enum.Enum): User's progress state
   - ACTIVE = "active" (currently reading)
   - PAUSED = "paused" (temporarily stopped)
   - COMPLETED = "completed" (reached an ending)
   - ABANDONED = "abandoned" (explicitly abandoned)
   Add display_name property and emoji property

Follow existing enum patterns in the file:
- Use class Name(str, Enum) pattern
- Implement __str__ method returning self.value
- Add display_name property with Spanish names
- Add emoji property where appropriate
  </action>
  <verify>grep -E "class StoryStatus|class NodeType|class StoryProgressStatus" bot/database/enums.py</verify>
  <done>All three enums exist with required values and follow existing patterns</done>
</task>

<task type="auto">
  <name>Task 2: Create Story model</name>
  <files>bot/database/models.py</files>
  <action>
Add the Story model class to bot/database/models.py after the last existing model (UserReward). Import StoryStatus enum at the top of the file.

Story model fields:
- id: Integer, primary_key=True, autoincrement=True
- title: String(200), nullable=False
- description: String(1000), nullable=True
- is_premium: Boolean, nullable=False, default=False (True = Premium/levels 4-6, False = Free/levels 1-3)
- status: Enum(StoryStatus), nullable=False, default=StoryStatus.DRAFT
- created_at: DateTime, nullable=False, default=utcnow lambda
- updated_at: DateTime, nullable=False, default=utcnow lambda, onupdate=utcnow lambda

Relationships:
- nodes: relationship to StoryNode with back_populates="story", cascade="all, delete-orphan", lazy="selectin"
- user_progress: relationship to UserStoryProgress with back_populates="story", cascade="all, delete-orphan", lazy="selectin"

Table args:
- Index on (status, is_premium) for filtering published stories by tier
- Index on (created_at) for sorting

Add __repr__ method returning f"<Story(id={self.id}, title='{self.title}', status={self.status.value})>"

Add property is_published returning self.status == StoryStatus.PUBLISHED
  </action>
  <verify>grep -A 5 "class Story" bot/database/models.py | head -20</verify>
  <done>Story model exists with all required fields, relationships, and indexes</done>
</task>

<task type="auto">
  <name>Task 3: Create StoryNode model</name>
  <files>bot/database/models.py</files>
  <action>
Add the StoryNode model class to bot/database/models.py after the Story model.

StoryNode model fields:
- id: Integer, primary_key=True, autoincrement=True
- story_id: Integer, ForeignKey("stories.id", ondelete="CASCADE"), nullable=False, index=True
- node_type: Enum(NodeType), nullable=False, default=NodeType.STORY
- content_text: String(4000), nullable=True (main narrative content)
- media_file_ids: JSON, nullable=True (list of Telegram file IDs for media)
- tier_required: Integer, nullable=False, default=1 (1-3 for Free, 4-6 for Premium)
- order_index: Integer, nullable=False, default=0 (for ordering nodes within story)
- is_active: Boolean, nullable=False, default=True, index=True
- created_at: DateTime, nullable=False, default=utcnow lambda
- updated_at: DateTime, nullable=False, default=utcnow lambda, onupdate=utcnow lambda

Relationships:
- story: relationship to Story with back_populates="nodes", lazy="selectin"
- choices: relationship to StoryChoice with foreign_keys="StoryChoice.source_node_id", back_populates="source_node", cascade="all, delete-orphan", lazy="selectin"
- incoming_choices: relationship to StoryChoice with foreign_keys="StoryChoice.target_node_id", back_populates="target_node", lazy="selectin"

Table args:
- Index on (story_id, order_index) for ordering nodes within a story
- Index on (story_id, node_type) for finding start/ending nodes

Add __repr__ method returning f"<StoryNode(id={self.id}, story={self.story_id}, type={self.node_type.value})>"

Add property has_media returning bool(self.media_file_ids)
  </action>
  <verify>grep -A 5 "class StoryNode" bot/database/models.py | head -20</verify>
  <done>StoryNode model exists with all required fields, self-referential relationships, and indexes</done>
</task>

<task type="auto">
  <name>Task 4: Create StoryChoice model</name>
  <files>bot/database/models.py</files>
  <action>
Add the StoryChoice model class to bot/database/models.py after the StoryNode model.

StoryChoice model fields:
- id: Integer, primary_key=True, autoincrement=True
- source_node_id: Integer, ForeignKey("story_nodes.id", ondelete="CASCADE"), nullable=False, index=True
- target_node_id: Integer, ForeignKey("story_nodes.id", ondelete="CASCADE"), nullable=False, index=True
- choice_text: String(500), nullable=False (text shown to user for this choice)
- cost_besitos: Integer, nullable=False, default=0 (cost to make this choice)
- conditions: JSON, nullable=True (optional conditions for showing this choice)
- is_active: Boolean, nullable=False, default=True, index=True
- created_at: DateTime, nullable=False, default=utcnow lambda

Relationships:
- source_node: relationship to StoryNode with foreign_keys=[source_node_id], back_populates="choices", lazy="selectin"
- target_node: relationship to StoryNode with foreign_keys=[target_node_id], back_populates="incoming_choices", lazy="selectin"

Table args:
- Index on (source_node_id, is_active) for finding active choices from a node

Add __repr__ method returning f"<StoryChoice(id={self.id}, source={self.source_node_id}, target={self.target_node_id})>"

Add property is_free returning self.cost_besitos == 0
  </action>
  <verify>grep -A 5 "class StoryChoice" bot/database/models.py | head -20</verify>
  <done>StoryChoice model exists with self-referential foreign keys and all required fields</done>
</task>

<task type="auto">
  <name>Task 5: Create UserStoryProgress model</name>
  <files>bot/database/models.py</files>
  <action>
Add the UserStoryProgress model class to bot/database/models.py after the StoryChoice model.

UserStoryProgress model fields:
- id: Integer, primary_key=True, autoincrement=True
- user_id: BigInteger, ForeignKey("users.user_id", ondelete="CASCADE"), nullable=False, index=True
- story_id: Integer, ForeignKey("stories.id", ondelete="CASCADE"), nullable=False, index=True
- current_node_id: Integer, ForeignKey("story_nodes.id", ondelete="SET NULL"), nullable=True, index=True
- decisions_made: JSON, nullable=False, default=list (array of {choice_id, node_id, made_at})
- status: Enum(StoryProgressStatus), nullable=False, default=StoryProgressStatus.ACTIVE
- started_at: DateTime, nullable=False, default=utcnow lambda
- completed_at: DateTime, nullable=True
- ending_reached: String(100), nullable=True (identifier of which ending was reached)
- updated_at: DateTime, nullable=False, default=utcnow lambda, onupdate=utcnow lambda

Relationships:
- user: relationship to User with uselist=False, lazy="selectin"
- story: relationship to Story with back_populates="user_progress", lazy="selectin"
- current_node: relationship to StoryNode with foreign_keys=[current_node_id], lazy="selectin"

Table args:
- Unique index on (user_id, story_id) - one progress record per user-story pair
- Index on (user_id, status) for finding active stories for a user
- Index on (story_id, status) for analytics

Add __repr__ method returning f"<UserStoryProgress(id={self.id}, user={self.user_id}, story={self.story_id}, status={self.status.value})>"

Add property is_completed returning self.status == StoryProgressStatus.COMPLETED
Add property decision_count returning len(self.decisions_made) if decisions_made else 0
  </action>
  <verify>grep -A 5 "class UserStoryProgress" bot/database/models.py | head -20</verify>
  <done>UserStoryProgress model exists with all required fields, relationships, and unique constraint</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run: grep -E "class (StoryStatus|NodeType|StoryProgressStatus)" bot/database/enums.py
2. Run: grep -E "class (Story|StoryNode|StoryChoice|UserStoryProgress)" bot/database/models.py
3. Verify all four models have proper imports at top of models.py
4. Verify enums are imported in models.py
</verification>

<success_criteria>
- StoryStatus, NodeType, StoryProgressStatus enums exist with all required values
- Story model exists with title, description, is_premium, status fields
- StoryNode model exists with story_id, node_type, content_text, media_file_ids, tier_required fields
- StoryChoice model exists with source_node_id, target_node_id, choice_text, cost_besitos fields
- UserStoryProgress model exists with user_id, story_id, current_node_id, decisions_made, status fields
- All relationships defined (Story->StoryNode, StoryNode->StoryChoice, UserStoryProgress->Story/StoryNode)
- Proper indexes created for query optimization
</success_criteria>

<output>
After completion, create `.planning/phases/27-core-narrative-engine/27-01-SUMMARY.md`
</output>
