---
phase: 29-admin-story-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/database/models.py
  - bot/database/enums.py
autonomous: true

must_haves:
  truths:
    - "Node conditions can be stored in database with type and value"
    - "Nodes can have multiple rewards attached"
    - "PRODUCT_OWNED condition type exists for shop product checks"
    - "NodeCondition uses same pattern as RewardCondition"
  artifacts:
    - path: "bot/database/enums.py"
      provides: "RewardConditionType with PRODUCT_OWNED"
      contains: "PRODUCT_OWNED"
    - path: "bot/database/models.py"
      provides: "NodeCondition model"
      contains: "class NodeCondition"
    - path: "bot/database/models.py"
      provides: "NodeReward junction table"
      contains: "class NodeReward"
  key_links:
    - from: "NodeCondition"
      to: "StoryNode"
      via: "ForeignKey on node_id"
      pattern: "node_id.*story_nodes.id"
    - from: "NodeReward"
      to: "StoryNode"
      via: "ForeignKey on node_id"
      pattern: "node_id.*story_nodes.id"
    - from: "NodeReward"
      to: "Reward"
      via: "ForeignKey on reward_id"
      pattern: "reward_id.*rewards.id"
---

<objective>
Extend database models to support node-level conditions and rewards.

Purpose: Enable the admin story editor to configure access conditions for individual nodes and attach rewards that trigger when users reach nodes.
Output: NodeCondition model, NodeReward junction table, PRODUCT_OWNED condition type
</objective>

<execution_context>
@/data/data/com.termux/files/home/.claude/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-core-narrative-engine/27-01-SUMMARY.md
@bot/database/models.py
@bot/database/enums.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PRODUCT_OWNED to RewardConditionType enum</name>
  <files>bot/database/enums.py</files>
  <action>
    Add PRODUCT_OWNED = ("product_owned", "Producto Comprado", "🛒") to RewardConditionType enum.
    This enables checking if user owns a specific shop product as a condition for node access.
    Place it after BESITOS_SPENT in the enum definition to maintain logical grouping.
  </action>
  <verify>grep -n "PRODUCT_OWNED" bot/database/enums.py</verify>
  <done>PRODUCT_OWNED exists in RewardConditionType enum with proper tuple values</done>
</task>

<task type="auto">
  <name>Task 2: Create NodeCondition model</name>
  <files>bot/database/models.py</files>
  <action>
    Create NodeCondition class after StoryChoice model (around line 1750).
    Model structure:
    - id: Integer primary key
    - node_id: ForeignKey to story_nodes.id with CASCADE delete
    - condition_type: Enum(RewardConditionType) - reuse existing enum
    - condition_value: Integer nullable (the threshold/value)
    - condition_group: Integer default=0 (0=AND group, 1+=OR groups)
    - is_active: Boolean default=True
    - created_at: DateTime default=utcnow

    Add relationship in StoryNode: conditions = relationship("NodeCondition", back_populates="node")
    Add relationship in NodeCondition: node = relationship("StoryNode", back_populates="conditions")

    Follow the exact same pattern as RewardCondition model for consistency.
  </action>
  <verify>grep -n "class NodeCondition" bot/database/models.py</verify>
  <done>NodeCondition model exists with all required fields and relationships</done>
</task>

<task type="auto">
  <name>Task 3: Create NodeReward junction table</name>
  <files>bot/database/models.py</files>
  <action>
    Create NodeReward class after NodeCondition model.
    This is a junction table linking StoryNode to Reward (many-to-many).

    Model structure:
    - id: Integer primary key
    - node_id: ForeignKey to story_nodes.id with CASCADE delete
    - reward_id: ForeignKey to rewards.id with CASCADE delete
    - is_active: Boolean default=True (allows disabling without deleting)
    - created_at: DateTime default=utcnow

    Add relationship in StoryNode: attached_rewards = relationship("NodeReward", back_populates="node")
    Add relationship in Reward: node_rewards = relationship("NodeReward", back_populates="reward")
    Add relationships in NodeReward: node = relationship("StoryNode"), reward = relationship("Reward")

    Add unique constraint on (node_id, reward_id) to prevent duplicates.
  </action>
  <verify>grep -n "class NodeReward" bot/database/models.py</verify>
  <done>NodeReward junction table exists linking StoryNode and Reward</done>
</task>

</tasks>

<verification>
1. Run Python syntax check: python -m py_compile bot/database/models.py bot/database/enums.py
2. Verify imports work: python -c "from bot.database.models import NodeCondition, NodeReward; from bot.database.enums import RewardConditionType; print(RewardConditionType.PRODUCT_OWNED)"
3. Check all relationships are defined correctly
</verification>

<success_criteria>
- PRODUCT_OWNED added to RewardConditionType enum
- NodeCondition model created with same pattern as RewardCondition
- NodeReward junction table created for many-to-many relationship
- All relationships defined (StoryNode.conditions, StoryNode.attached_rewards)
- Python syntax valid, imports work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/29-admin-story-editor/29-01-SUMMARY.md`
</output>
