---
phase: 29-admin-story-editor
plan: 04
type: execute
wave: 3
depends_on: ["29-02", "29-03"]
files_modified:
  - bot/handlers/admin/story_management.py
  - bot/handlers/admin/main.py
  - bot/utils/keyboards.py
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Admin can see validation status in story list"
    - "Validation runs before publishing and blocks if critical errors"
    - "Admin can preview story as user would see it"
    - "Story editor accessible from admin main menu"
    - "All ADMIN-01 through ADMIN-12 requirements satisfied"
  artifacts:
    - path: "bot/handlers/admin/story_management.py"
      provides: "Validation display and preview mode"
      contains: "callback_story_preview"
    - path: "bot/handlers/admin/main.py"
      provides: "Admin menu integration"
      contains: "admin:stories"
  key_links:
    - from: "story list"
      to: "validation"
      via: "StoryEditorService.validate_story"
      pattern: "validate_story"
    - from: "preview mode"
      to: "NarrativeService"
      via: "container.narrative"
      pattern: "container.narrative"
---

<objective>
Complete admin story editor with validation display, preview mode, and menu integration.

Purpose: Finalize the admin story editor by adding validation status visualization, story preview functionality, and integrating into the admin main menu.
Output: Complete story_management.py with validation and preview, updated admin menu
</objective>

<execution_context>
@/data/data/com.termux/files/home/.claude/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-core-narrative-engine/27-03-SUMMARY.md
@bot/handlers/admin/story_management.py
@bot/handlers/admin/main.py
@bot/services/story_editor.py
@bot/services/narrative.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement validation status display</name>
  <files>bot/handlers/admin/story_management.py</files>
  <action>
    Add validation display functionality:

    1. format_validation_status(is_valid, errors, info) helper
       - Returns emoji + text status: "✅ Jugable", "⚠️ Revisar", "❌ Bloqueado"
       - Shows error count if any
       - Format: "🟢 Publicada (✅ Jugable)" or "🟡 Borrador (⚠️ 2 warnings)"

    2. Update callback_story_list to show validation badges
       - For each story, run validate_story() or use cached result
       - Show status badge next to story title
       - Color code: green=valid, yellow=warnings, red=errors

    3. callback_story_validation_detail - Show full validation report
       - Display all errors with "❌" prefix
       - Display all warnings with "⚠️" prefix
       - Show info: total nodes, endings, unreachable nodes
       - Button to fix issues (go to node editor)

    4. Update callback_story_publish to enforce validation
       - Before publishing, always run validate_story()
       - If errors exist: show error list, block publish
       - If only warnings: show confirmation with warning list
       - Only allow publish if clean or force=True

    Validation runs on-demand, not cached (stories change frequently during editing).
  </action>
  <verify>grep -n "format_validation_status\|validate_story" bot/handlers/admin/story_management.py | head -20</verify>
  <done>Validation status displays in story list and blocks publish on critical errors</done>
</task>

<task type="auto">
  <name>Task 2: Implement story preview mode</name>
  <files>bot/handlers/admin/story_management.py</files>
  <action>
    Add preview mode for testing stories:

    1. callback_story_preview - Start preview mode
       - Use NarrativeService to simulate user experience
       - Call container.narrative.get_current_node(admin_user_id, story_id)
       - Display node exactly as user would see it
       - Show Diana's voice (🫦) for content (as users see)

    2. callback_preview_choice - Handle choice in preview
       - Call container.narrative.make_choice()
       - Show next node
       - Continue until ending or exit

    3. callback_preview_exit - Exit preview mode
       - Clear preview state
       - Return to story details
       - Note: Don't save progress (preview is transient)

    4. Preview state management
       - Store preview_story_id in FSM
       - Track preview_current_node_id
       - Separate from actual user progress

    Preview uses the actual NarrativeService methods but with a flag to not persist.
    Or create a preview mode that doesn't save to UserStoryProgress.
  </action>
  <verify>grep -n "callback_story_preview\|container.narrative" bot/handlers/admin/story_management.py</verify>
  <done>Preview mode allows admin to experience story as users would</done>
</task>

<task type="auto">
  <name>Task 3: Add admin menu integration</name>
  <files>bot/handlers/admin/main.py</files>
  <action>
    Integrate story editor into admin main menu:

    1. Add "📖 Historias" button to admin main menu
       - Position after "📦 Paquetes" or in logical group
       - callback_data: "admin:stories"
       - Use book emoji for stories

    2. Update admin main menu text
       - Add description: "📖 Historias - Crear y gestionar historias narrativas"

    3. Ensure proper routing
       - callback "admin:stories" routes to callback_stories_menu
       - Already implemented in story_management.py

    The menu integration makes the story editor discoverable from main admin interface.
  </action>
  <verify>grep -n "admin:stories\|📖 Historias" bot/handlers/admin/main.py</verify>
  <done>Story editor accessible from admin main menu with "📖 Historias" button</done>
</task>

<task type="auto">
  <name>Task 4: Add keyboard utilities for story editor</name>
  <files>bot/utils/keyboards.py</files>
  <action>
    Add keyboard helper functions for story management:

    1. get_story_management_keyboard() - Main menu keyboard
       - Buttons: "Crear Historia", "Listar Historias", "Volver"

    2. get_story_list_keyboard(stories, page=0) - Paginated story list
       - One button per story: "{title} ({status_emoji})"
       - callback: "admin:story:details:{id}"
       - Pagination: "⬅️ Anterior", "➡️ Siguiente"
       - "Crear Nueva" button at bottom

    3. get_story_detail_keyboard(story, is_valid) - Story detail actions
       - Buttons: "Editar", "Publicar"/"Despublicar", "Preview", "Ver Nodos", "Eliminar", "Volver"
       - Publish button disabled if not valid

    4. get_node_list_keyboard(nodes, story_id) - Node list
       - One button per node: "{type_emoji} {preview}"
       - callback: "admin:node:edit:{id}"
       - "Agregar Nodo" button

    5. get_node_edit_keyboard(node) - Node edit actions
       - Buttons: "Editar Contenido", "Condiciones", "Recompensas", "Ver Elecciones", "Eliminar", "Volver"

    6. get_choice_list_keyboard(choices, node_id) - Choice list
       - One button per choice: "{text} → {target}"
       - "Agregar Elección" button

    Follow existing keyboard patterns in the file.
  </action>
  <verify>grep -n "get_story_management_keyboard\|get_node_list_keyboard" bot/utils/keyboards.py</verify>
  <done>Keyboard utilities added for all story editor screens</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 5: Verify complete admin story editor flow</name>
  <what-built>
    Complete admin story editor with:
    - Story list with validation badges
    - Story creation wizard (title → description → premium)
    - Node creation wizard (content → type → conditions → rewards → choices)
    - Inline reward creation with checkpoint/resume
    - Choice creation with node linking
    - Edit/delete for stories, nodes, choices
    - Validation display and publish blocking
    - Preview mode
    - Admin menu integration
  </what-built>
  <how-to-verify>
    1. Start bot and access admin panel
    2. Click "📖 Historias" button
    3. Create a new story:
       - Enter title: "Test Story"
       - Enter description: "A test story"
       - Select "Premium: No"
    4. Create first node:
       - Send text content: "Welcome to the story"
       - Select type: "Inicio"
       - Skip conditions
       - Skip rewards
       - Select "No, crear elecciones"
    5. Create a choice:
       - Text: "Continue"
       - Select "Crear nuevo nodo"
       - Create ending node with "The End" content
    6. View story list and verify validation shows "✅ Jugable"
    7. Click "Preview" and verify you can navigate the story
    8. Publish the story
    9. Verify story appears in user story list
  </how-to-verify>
  <resume-signal>Type "approved" if all flows work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. All handlers registered and reachable from admin menu
2. Validation runs before publish and blocks on errors
3. Preview mode shows story as users see it
4. All ADMIN-01 through ADMIN-12 requirements verified
</verification>

<success_criteria>
- Validation status shows in story list (✅ Jugable, ⚠️ Revisar, ❌ Bloqueado)
- Publishing blocked if critical validation errors exist
- Preview mode allows admin to test stories
- "📖 Historias" button in admin main menu
- All 12 ADMIN requirements satisfied
- Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/29-admin-story-editor/29-04-SUMMARY.md`
</output>
