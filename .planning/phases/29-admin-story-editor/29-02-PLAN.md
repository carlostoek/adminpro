---
phase: 29-admin-story-editor
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - bot/handlers/admin/story_management.py
  - bot/states/admin.py
  - bot/handlers/admin/__init__.py
autonomous: true

must_haves:
  truths:
    - "Admin can see list of all stories with status badges"
    - "Admin can create new story with title, description, premium flag"
    - "Admin can edit story metadata (title, description, premium)"
    - "Admin can publish/unpublish stories"
    - "Admin can soft-delete draft stories"
    - "Admin can view story statistics"
  artifacts:
    - path: "bot/handlers/admin/story_management.py"
      provides: "Story management handlers"
      min_lines: 400
    - path: "bot/states/admin.py"
      provides: "StoryEditorStates FSM states"
      contains: "class StoryEditorStates"
  key_links:
    - from: "story_management.py"
      to: "StoryEditorService"
      via: "ServiceContainer.story_editor"
      pattern: "container.story_editor"
    - from: "admin menu"
      to: "story_management"
      via: "callback_data: admin:stories"
      pattern: "admin:stories"
---

<objective>
Implement story management handlers for admin CRUD operations.

Purpose: Enable admins to list, create, edit, publish, and delete stories through the Telegram bot interface.
Output: story_management.py with story CRUD handlers, StoryEditorStates FSM states
</objective>

<execution_context>
@/data/data/com.termux/files/home/.claude/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-core-narrative-engine/27-03-SUMMARY.md
@bot/handlers/admin/reward_management.py
@bot/services/story_editor.py
@bot/states/admin.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StoryEditorStates to admin.py</name>
  <files>bot/states/admin.py</files>
  <action>
    Add StoryEditorStates StatesGroup after ContentSetCreateState.

    States needed:
    - waiting_for_title: Story creation - title input
    - waiting_for_description: Story creation - description input
    - waiting_for_premium: Story creation - premium flag selection
    - waiting_for_edit_field: Story edit - field selection
    - waiting_for_edit_value: Story edit - new value input
    - waiting_for_publish_confirm: Publish confirmation
    - waiting_for_delete_confirm: Delete confirmation

    Each state needs docstring explaining its purpose in the flow.
    Follow the pattern of other StatesGroup classes in the file.
  </action>
  <verify>grep -n "class StoryEditorStates" bot/states/admin.py</verify>
  <done>StoryEditorStates class exists with all required states</done>
</task>

<task type="auto">
  <name>Task 2: Create story_management.py with list and menu handlers</name>
  <files>bot/handlers/admin/story_management.py</files>
  <action>
    Create new file bot/handlers/admin/story_management.py.

    Implement handlers:
    1. callback_stories_menu - Main story management menu
       - Shows "Crear Historia", "Listar Historias", "Volver" buttons
       - Uses Lucien voice (🎩)

    2. callback_story_list - Paginated story list
       - Query all stories from database
       - Show title + node count + validation status
       - Status badges: "🟢 Publicada", "🟡 Borrador", "⚠️ Revisar"
       - Each story has "Editar" button (callback: admin:story:edit:{id})
       - Add "Crear Nueva" button at bottom

    3. callback_story_details - Show story detail view
       - Display title, description, status, node count
       - Buttons: Editar, Publicar/Despublicar, Eliminar, Volver

    Use StoryEditorService from ServiceContainer for data operations.
    All messages use Lucien voice (🎩) with elegant mayordomo tone.
    Import and use create_inline_keyboard from bot.utils.keyboards.
  </action>
  <verify>ls -la bot/handlers/admin/story_management.py</verify>
  <done>story_management.py exists with menu, list, and detail handlers</done>
</task>

<task type="auto">
  <name>Task 3: Implement story creation flow</name>
  <files>bot/handlers/admin/story_management.py</files>
  <action>
    Add story creation handlers to story_management.py:

    1. callback_story_create_start - Start creation flow
       - Set state to waiting_for_title
       - Prompt for title with cancel button

    2. process_story_title - Handle title input
       - Validate: 1-200 chars, not empty
       - Store in FSM data, advance to waiting_for_description
       - Prompt for description with /skip option

    3. process_story_description - Handle description
       - Allow /skip for empty description
       - Validate: max 1000 chars if provided
       - Store in FSM, advance to waiting_for_premium
       - Show premium selection buttons: [Sí, Premium] [No, Free]

    4. callback_story_premium_selected - Handle premium selection
       - Get is_premium from callback_data
       - Call StoryEditorService.create_story()
       - Show success message with story ID
       - IMMEDIATELY start node wizard (transition to node creation)
       - Call callback_node_create_start with the new story_id

    All handlers use proper FSM state management.
    Include cancel buttons at each step.
  </action>
  <verify>grep -n "create_story" bot/handlers/admin/story_management.py</verify>
  <done>Story creation flow implemented with 4-step wizard</done>
</task>

<task type="auto">
  <name>Task 4: Implement story edit, publish, delete handlers</name>
  <files>bot/handlers/admin/story_management.py</files>
  <action>
    Add remaining management handlers:

    1. callback_story_edit - Show edit menu
       - Display current story data
       - Buttons: Editar Título, Editar Descripción, Cambiar Premium, Volver

    2. callback_story_edit_field - Handle field selection
       - Set state to waiting_for_edit_value
       - Store field being edited in FSM
       - Prompt for new value

    3. process_story_edit_value - Handle new value input
       - Validate based on field type
       - Update story via StoryEditorService
       - Return to story details view

    4. callback_story_publish - Toggle publish status
       - If draft: run validation first, show errors if any
       - If published: confirm unpublish
       - Use StoryEditorService.publish_story()

    5. callback_story_delete - Soft delete story
       - Only allow deleting DRAFT stories
       - Show confirmation with warning
       - Set is_active=False (soft delete)

    6. callback_story_stats - Show story statistics
       - Call StoryEditorService.get_story_stats()
       - Display: total nodes, user starts, completions, completion rate

    All handlers follow existing patterns from reward_management.py.
  </action>
  <verify>grep -n "callback_story_" bot/handlers/admin/story_management.py | wc -l</verify>
  <done>Edit, publish, delete, stats handlers implemented</done>
</task>

<task type="auto">
  <name>Task 5: Register router in admin __init__.py</name>
  <files>bot/handlers/admin/__init__.py</files>
  <action>
    Import and register story_router in bot/handlers/admin/__init__.py.

    Add import: from bot.handlers.admin.story_management import story_router
    Include story_router in the list of routers to include.
    Add "Crear historia" button to admin main menu (in main.py or menu.py).
    The button should callback to "admin:stories".
  </action>
  <verify>grep -n "story_router" bot/handlers/admin/__init__.py</verify>
  <done>story_router registered and menu button added</done>
</task>

</tasks>

<verification>
1. Python syntax check: python -m py_compile bot/handlers/admin/story_management.py
2. Import check: python -c "from bot.handlers.admin.story_management import story_router"
3. State check: python -c "from bot.states.admin import StoryEditorStates; print(StoryEditorStates.waiting_for_title)"
4. Router registration: grep story_router bot/handlers/admin/__init__.py
</verification>

<success_criteria>
- StoryEditorStates added to admin.py with all required states
- story_management.py created with 15+ handlers
- Story list shows status badges and node counts
- Story creation wizard works (title → description → premium)
- Edit, publish, delete handlers functional
- Router registered in admin __init__.py
- "Crear historia" button in admin menu
</success_criteria>

<output>
After completion, create `.planning/phases/29-admin-story-editor/29-02-SUMMARY.md`
</output>
