---
phase: 29-admin-story-editor
plan: 03
type: execute
wave: 3
depends_on: ["29-01", "29-02"]
files_modified:
  - bot/handlers/admin/story_management.py
  - bot/services/story_editor.py
autonomous: true

must_haves:
  truths:
    - "Admin can create nodes via wizard with content input"
    - "Admin can configure conditions for node access"
    - "Admin can attach rewards to nodes"
    - "Admin can create choices linking nodes"
    - "Admin can mark nodes as final"
    - "Inline reward creation works with checkpoint/resume"
  artifacts:
    - path: "bot/handlers/admin/story_management.py"
      provides: "Node and choice wizard handlers"
      contains: "callback_node_create_start"
    - path: "bot/services/story_editor.py"
      provides: "Extended StoryEditorService methods"
      contains: "create_node_condition"
  key_links:
    - from: "node wizard"
      to: "condition system"
      via: "reuse RewardConditionState"
      pattern: "RewardConditionState"
    - from: "node wizard"
      to: "reward creation"
      via: "checkpoint/resume pattern"
      pattern: "checkpoint_state"
---

<objective>
Implement node and choice creation wizard with conditions and rewards.

Purpose: Enable admins to build story structure by creating nodes with content, configuring access conditions, attaching rewards, and linking nodes with choices.
Output: Extended story_management.py with node/choice wizard, checkpoint/resume for inline reward creation
</objective>

<execution_context>
@/data/data/com.termux/files/home/.claude/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-admin-story-editor/29-RESEARCH.md
@bot/handlers/admin/story_management.py
@bot/handlers/admin/reward_management.py
@bot/services/story_editor.py
@bot/states/admin.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend StoryEditorService for node conditions and rewards</name>
  <files>bot/services/story_editor.py</files>
  <action>
    Add methods to StoryEditorService:

    1. create_node_condition(node_id, condition_type, condition_value, condition_group=0)
       - Create NodeCondition record
       - Return (bool, str, Optional[NodeCondition])

    2. attach_reward_to_node(node_id, reward_id)
       - Create NodeReward junction record
       - Check for duplicates
       - Return (bool, str)

    3. detach_reward_from_node(node_id, reward_id)
       - Set NodeReward is_active=False
       - Return (bool, str)

    4. get_node_conditions(node_id) -> List[NodeCondition]
       - Return all conditions for a node

    5. get_node_rewards(node_id) -> List[Reward]
       - Return all rewards attached to node via NodeReward

    6. update_node(node_id, **kwargs)
       - Update node fields (content_text, media_file_ids, tier_required, etc.)
       - Return (bool, str, Optional[StoryNode])

    7. delete_node(node_id)
       - Soft delete (set is_active=False)
       - Cascade to choices
       - Return (bool, str)

    8. update_choice(choice_id, **kwargs)
       - Update choice fields
       - Return (bool, str, Optional[StoryChoice])

    9. delete_choice(choice_id)
       - Soft delete choice
       - Return (bool, str)

    Follow existing patterns: async methods, no commits, tuple returns.
  </action>
  <verify>grep -n "create_node_condition\|attach_reward_to_node" bot/services/story_editor.py</verify>
  <done>StoryEditorService extended with 9 new methods for node/condition/reward management</done>
</task>

<task type="auto">
  <name>Task 2: Implement node creation wizard - content input</name>
  <files>bot/handlers/admin/story_management.py</files>
  <action>
    Add node creation handlers (part 1 - content):

    1. callback_node_create_start - Start node wizard
       - Set state to waiting_for_content
       - Store story_id in FSM
       - Prompt: "Envíe el contenido del nodo (texto, foto o video)"
       - Show cancel button

    2. process_node_content - Handle content input
       - Accept: text, photo, video
       - For photo: use message.photo[-1].file_id, caption as content_text
       - For video: use message.video.file_id, caption as content_text
       - For text: content_text = message.text
       - Validate: at least text OR media must exist
       - Store: node_content_text, node_media_file_ids, node_media_type
       - Advance to waiting_for_node_type
       - Ask: "¿Qué tipo de nodo es?" with buttons: [Inicio] [Historia] [Decisión] [Final]

    3. callback_node_type_selected - Handle type selection
       - Store node_type in FSM
       - If type is ENDING: skip to final confirmation
       - Else: advance to waiting_for_conditions
       - Ask: "¿Este nodo tiene condiciones de acceso?"

    All handlers use Lucien voice (🎩).
    Follow content_set_management.py media handling pattern.
  </action>
  <verify>grep -n "process_node_content\|callback_node_type_selected" bot/handlers/admin/story_management.py</verify>
  <done>Node content input handlers implemented with media support</done>
</task>

<task type="auto">
  <name>Task 3: Implement condition configuration flow</name>
  <files>bot/handlers/admin/story_management.py</files>
  <action>
    Add condition configuration handlers:

    1. callback_node_conditions_question - Ask about conditions
       - Buttons: [Sí, configurar] [No, continuar]
       - If "No": skip to rewards question

    2. callback_node_conditions_start - Start condition setup
       - Reuse RewardConditionState.waiting_for_type
       - Store return_to="node_wizard" in FSM
       - Show condition type selection (reuse from reward_management.py)
       - Types: LEVEL_REACHED, TIER_REQUIRED, STREAK_LENGTH, TOTAL_POINTS, PRODUCT_OWNED

    3. callback_node_condition_type - Handle type selection
       - Store condition_type
       - Advance to RewardConditionState.waiting_for_value
       - Prompt for threshold value

    4. process_node_condition_value - Handle value input
       - Validate numeric input
       - Ask about condition group (AND/OR)
       - Store condition

    5. callback_node_condition_done - Finish conditions
       - Save all conditions via StoryEditorService.create_node_condition()
       - Advance to waiting_for_rewards

    Reuse existing condition UI from reward_management.py where possible.
    Use same validation logic for condition values.
  </action>
  <verify>grep -n "callback_node_condition" bot/handlers/admin/story_management.py</verify>
  <done>Condition configuration flow integrated with existing system</done>
</task>

<task type="auto">
  <name>Task 4: Implement reward configuration with inline creation</name>
  <files>bot/handlers/admin/story_management.py</files>
  <action>
    Add reward configuration handlers:

    1. callback_node_rewards_question - Ask about rewards
       - Show existing rewards list with checkboxes
       - Button: "➕ Crear nueva recompensa"
       - Button: "✅ Continuar" (when done)

    2. callback_node_reward_toggle - Toggle reward selection
       - Add/remove reward from selected list in FSM
       - Refresh reward list display

    3. callback_node_reward_create_inline - Start inline reward creation
       - CHECKPOINT PATTERN:
         a. Save current FSM state as checkpoint_state="waiting_for_rewards"
         b. Save all node data as checkpoint_data
         c. Set return_to="story_editor", return_callback="node:reward:created"
       - Switch to RewardCreateState.waiting_for_name
       - Launch reward creation wizard

    4. callback_node_reward_created - Resume after reward creation
       - Get created_reward_id from FSM
       - Restore checkpoint_data
       - Return to waiting_for_rewards state
       - Auto-select the newly created reward
       - Show reward list again

    5. callback_node_rewards_done - Finish rewards
       - Save all selected rewards via StoryEditorService.attach_reward_to_node()
       - Advance to waiting_for_final_decision

    Checkpoint pattern is critical - user must not lose node wizard progress.
  </action>
  <verify>grep -n "checkpoint_state\|callback_node_reward_created" bot/handlers/admin/story_management.py</verify>
  <done>Reward configuration with inline creation and checkpoint/resume implemented</done>
</task>

<task type="auto">
  <name>Task 5: Implement final node decision and choice creation</name>
  <files>bot/handlers/admin/story_management.py</files>
  <action>
    Add final node handlers and choice creation:

    1. callback_node_final_question - Ask if node is final
       - Buttons: [Sí, es final] [No, crear elecciones]
       - If final: mark as ENDING type, save node, show completion

    2. callback_node_create_choices - Start choice creation
       - Set state to waiting_for_choice_text
       - Prompt: "Texto para la opción de elección:"

    3. process_choice_text - Handle choice text
       - Validate: 1-500 chars
       - Store in FSM
       - Advance to waiting_for_target_node
       - Show: existing nodes list + "Crear nuevo nodo"

    4. callback_choice_target_selected - Handle target selection
       - If "Crear nuevo nodo": store choice_text, call callback_node_create_start
       - If existing node: store target_node_id
       - Advance to waiting_for_cost_besitos
       - Ask: "¿Costo en besitos? (0 = gratis)"

    5. process_choice_cost - Handle cost input
       - Validate: integer >= 0
       - Create choice via StoryEditorService.create_choice()
       - Ask: "¿Crear otra elección?"
       - Buttons: [Sí] [No, terminar nodo]

    6. callback_choice_create_another - Handle repeat choice
       - If yes: back to waiting_for_choice_text
       - If no: show node completion summary

    After node completion, offer: "Crear siguiente nodo" or "Volver al panel".
  </action>
  <verify>grep -n "process_choice_text\|callback_choice_target" bot/handlers/admin/story_management.py</verify>
  <done>Choice creation flow implemented with support for linking to new/existing nodes</done>
</task>

<task type="auto">
  <name>Task 6: Implement node edit and delete handlers</name>
  <files>bot/handlers/admin/story_management.py</files>
  <action>
    Add node management handlers:

    1. callback_node_list - Show nodes for a story
       - List all nodes ordered by order_index
       - Show: type, preview of content, choice count
       - Each node has "Editar" button

    2. callback_node_edit - Show node edit menu
       - Display node content preview
       - Buttons: Editar Contenido, Gestionar Condiciones, Gestionar Recompensas, Ver Elecciones, Eliminar, Volver

    3. callback_node_edit_content - Edit node content
       - Set state to waiting_for_content
       - Pre-fill with existing content
       - On save: use StoryEditorService.update_node()

    4. callback_node_delete - Soft delete node
       - Show confirmation with warning about cascade
       - On confirm: StoryEditorService.delete_node()
       - Return to node list

    5. callback_choice_list - Show choices for a node
       - List all choices with target and cost
       - Buttons to edit/delete each choice

    6. callback_choice_edit - Edit choice
       - Allow editing text, target, cost
       - Use StoryEditorService.update_choice()

    7. callback_choice_delete - Delete choice
       - Confirm then soft delete
       - Use StoryEditorService.delete_choice()

    All edits maintain data integrity and use soft deletes.
  </action>
  <verify>grep -n "callback_node_edit\|callback_choice_list" bot/handlers/admin/story_management.py</verify>
  <done>Node and choice edit/delete handlers implemented</done>
</task>

</tasks>

<verification>
1. Syntax check: python -m py_compile bot/handlers/admin/story_management.py bot/services/story_editor.py
2. Import check: python -c "from bot.handlers.admin.story_management import story_router"
3. Method check: python -c "from bot.services.story_editor import StoryEditorService; print([m for m in dir(StoryEditorService) if not m.startswith('_')])"
</verification>

<success_criteria>
- StoryEditorService extended with 9 new methods
- Node creation wizard: content → type → conditions → rewards → final/choices
- Condition configuration reuses existing RewardCondition system
- Inline reward creation uses checkpoint/resume pattern
- Choice creation supports linking to existing or new nodes
- Node and choice edit/delete handlers functional
- All flows use Lucien voice (🎩)
</success_criteria>

<output>
After completion, create `.planning/phases/29-admin-story-editor/29-03-SUMMARY.md`
</output>
