# Telegram Bot Configuration
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_USER_IDS=123456789,987654321

# Environment
# Options: "production" or "development" (default: development)
# In production, migrations run automatically on startup
ENV=development

# Database
# Local development (SQLite)
# DATABASE_URL=sqlite+aiosqlite:///bot.db
# DATABASE_URL=sqlite:///bot.db

# Production (PostgreSQL on Railway)
# DATABASE_URL=postgresql://user:password@host.railway.app:5432/railway
# DATABASE_URL=postgresql+asyncpg://user:password@host.railway.app:5432/railway

# Default (fallback to SQLite if not set)
DATABASE_URL=sqlite+aiosqlite:///bot.db

# Health Check API
# Port that Railway expects the health check API to listen on
HEALTH_PORT=8000
# Host for health check API (default: 0.0.0.0 for external access)
HEALTH_HOST=0.0.0.0

# Channels (se configuran desde el bot)
VIP_CHANNEL_ID=
FREE_CHANNEL_ID=

# Creator
CREATOR_USERNAME=diana_artista

# Bot Settings
DEFAULT_WAIT_TIME_MINUTES=5
LOG_LEVEL=INFO

# Background Tasks
CLEANUP_INTERVAL_MINUTES=60
PROCESS_FREE_QUEUE_MINUTES=5

# ===== RAILWAY DEPLOYMENT =====
# These are set automatically by Railway when deployed
# Do NOT set these locally unless testing Railway behavior

# Port that Railway expects the app to listen on
# PORT=8000  # (set by Railway automatically)

# Environment (production/development)
# ENV=production  # Set to production on Railway for auto-migrations
